{
  "pluginAlias": "DihoolLifts",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "Control DIHOOL IPS-S2 scissor lifts via the eWeLink LAN protocol. Enter your eWeLink credentials for automatic device discovery, or configure devices manually with their Device ID and LAN Key.",
  "footerDisplay": "For help, see the [GitHub repository](https://github.com/alampros/homebridge-dihool-lifts).",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Plugin Name",
        "type": "string",
        "default": "Dihool Lifts",
        "required": true
      },
      "username": {
        "title": "eWeLink Email or Phone",
        "type": "string",
        "description": "Your eWeLink account email or phone number. Required for automatic device discovery.",
        "placeholder": "user@example.com"
      },
      "password": {
        "title": "eWeLink Password",
        "type": "string",
        "description": "Your eWeLink account password.",
        "x-schema-form": {
          "type": "password"
        }
      },
      "countryCode": {
        "title": "Country Code",
        "type": "string",
        "default": "+1",
        "description": "Your country calling code (e.g. +1 for US, +44 for UK).",
        "placeholder": "+1"
      },
      "mode": {
        "title": "Connection Mode",
        "type": "string",
        "default": "auto",
        "oneOf": [
          { "title": "Auto (cloud discovery + LAN control)", "enum": ["auto"] },
          { "title": "LAN Only (requires manual device config)", "enum": ["lan"] }
        ],
        "description": "In 'auto' mode, the plugin logs into eWeLink to discover devices, then controls them over LAN. In 'lan' mode, no cloud connection is made and each device entry must include a Device ID and LAN Key."
      },
      "debug": {
        "title": "Debug Logging",
        "type": "boolean",
        "default": false,
        "description": "Enable verbose debug logging."
      },
      "devices": {
        "title": "Lift Devices",
        "type": "array",
        "description": "Per-device configuration. Manual/LAN-only devices require a Device ID and LAN Key. With cloud discovery, devices are found automatically; use this section for travel times and other overrides. If you have exactly one lift, the Device ID may be omitted.",
        "items": {
          "type": "object",
          "properties": {
            "deviceId": {
              "title": "Device ID",
              "type": "string",
              "description": "The eWeLink device ID (e.g. 100293a98d). Required for manual/LAN-only devices and for matching overrides when you have multiple cloud-discovered lifts. Found in the eWeLink app under device settings, or in the Homebridge log on startup.",
              "placeholder": "100293a98d"
            },
            "label": {
              "title": "Display Name",
              "type": "string",
              "description": "Custom name for this lift in HomeKit.",
              "placeholder": "Sunroom Lift"
            },
            "operationTimeUp": {
              "title": "Raise Time (seconds)",
              "type": "number",
              "placeholder": "15",
              "minimum": 1,
              "maximum": 120,
              "description": "How long the motor runs to go from fully down (0%) to fully up (100%). Used only for the HomeKit status indicator timing. Time this with a stopwatch for accuracy. Default: 8s."
            },
            "operationTimeDown": {
              "title": "Lower Time (seconds)",
              "type": "number",
              "placeholder": "16",
              "minimum": 1,
              "maximum": 120,
              "description": "How long the motor runs to go from fully up (100%) to fully down (0%). Used only for the HomeKit status indicator timing. Default: 8s."
            },
            "lanKey": {
              "title": "LAN Key",
              "type": "string",
              "description": "Device encryption key. Required for manual/LAN-only devices. Cloud-discovered devices get this automatically. Use the get-lankey script to obtain this.",
              "x-schema-form": {
                "type": "password"
              }
            },
            "ipAddress": {
              "title": "IP Address Override",
              "type": "string",
              "description": "Manual IP address. Only needed if mDNS discovery doesn't find the device.",
              "placeholder": "192.168.1.100",
              "format": "ipv4"
            },
            "upChannel": {
              "title": "Up Channel",
              "type": "integer",
              "placeholder": "0",
              "minimum": 0,
              "maximum": 3,
              "description": "eWeLink outlet index for the UP motor control. Default: 0."
            },
            "downChannel": {
              "title": "Down Channel",
              "type": "integer",
              "placeholder": "1",
              "minimum": 0,
              "maximum": 3,
              "description": "eWeLink outlet index for the DOWN motor control. Default: 1."
            }
          }
        }
      }
    }
  },
  "layout": [
    {
      "type": "fieldset",
      "title": "eWeLink Account",
      "description": "Enter your eWeLink credentials for automatic device discovery.",
      "expandable": true,
      "expanded": true,
      "items": ["username", "password", "countryCode"]
    },
    {
      "key": "devices",
      "type": "array",
      "title": "Lift Devices",
      "expandable": true,
      "expanded": true,
      "startEmpty": true,
      "buttonText": "Add Lift",
      "items": [
        "devices[].deviceId",
        "devices[].label",
        {
          "type": "fieldset",
          "title": "Travel Times",
          "description": "Controls how long the motor runs for each direction. Time your lift with a stopwatch and enter the values here.",
          "expandable": false,
          "items": [
            "devices[].operationTimeUp",
            "devices[].operationTimeDown"
          ]
        },
        {
          "type": "fieldset",
          "title": "Network & Channels",
          "description": "Only change these if you know what you're doing.",
          "expandable": true,
          "expanded": false,
          "items": [
            "devices[].lanKey",
            "devices[].ipAddress",
            "devices[].upChannel",
            "devices[].downChannel"
          ]
        }
      ]
    },
    {
      "type": "fieldset",
      "title": "General Settings",
      "expandable": true,
      "expanded": false,
      "items": ["mode", "debug"]
    }
  ]
}
